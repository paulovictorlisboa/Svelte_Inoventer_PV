// import express from 'express';
// import { authMiddleware } from '../middlewares/auth';
// import { validatorMiddleware } from '../validation/validator'
// import * as schemas from '../validation/schemas';
// import { catchAsync } from '../utils/catchAsync';
// import { getOverlays } from '../controllers/overlay.controller';

// const router = express.Router();

// // route to get available overlays
// router.get('/', validatorMiddleware(schemas.AccessSchema), authMiddleware, catchAsync(getOverlays));

// export default router;
